<% if resource.new_record? %>
  $("#join-us").removeClass("link").html("<%= j render partial: "form" %>");
<% else %>
  $("#join-us").removeClass("link").html("<%= j render partial: "success" %>");
<% end %>
