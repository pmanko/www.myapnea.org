<%= form_for(@answer_option) do |f| %>
  <% if @answer_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_option.errors.count, "error") %> prohibited this answer_option from being saved:</h2>

      <ul>
      <% @answer_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numeric_value %><br>
    <%= f.text_field :numeric_value %>
  </div>
  <div class="field">
    <%= f.label :text_value %><br>
    <%= f.text_field :text_value %>
  </div>
  <div class="field">
    <%= f.label :time_value %><br>
    <%= f.datetime_select :time_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
