<%= form_for(@answer_option, :html => { :class => "form-horizontal" }) do |f| %>
  <% if @answer_option.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@answer_option.errors.count, "error") %> prohibited this answer_option from being saved:</h4>

      <ul>
      <% @answer_option.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :numeric_value, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :numeric_value, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :text_value, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :text_value, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :time_value, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.datetime_select :time_value, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
